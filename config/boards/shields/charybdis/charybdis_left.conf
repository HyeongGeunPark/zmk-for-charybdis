# Peripheral. The id distinguishes the two halves on the shared ESB address,
# and must stay <= CONFIG_ZMK_SPLIT_ESB_PERIPHERAL_COUNT on the dongle.
CONFIG_ZMK_SPLIT_ESB_PERIPHERAL_ID=1
CONFIG_ZMK_SPLIT_ESB_EVENT_BUFFER_ITEMS=64
